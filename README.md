# F5GAME.RU: Что вместо критики?

Интерактивный тренажер для обучения навыкам ответа на критику по методике Ольги Грищенко.

## 🚀 Деплой на Netlify

### Шаг 1: Загрузка на GitHub

1. Создай новый репозиторий на GitHub (например, `criticism-app`)
2. Загрузи все файлы из этой папки в репозиторий

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/criticism-app.git
git push -u origin main
```

### Шаг 2: Подключение к Netlify

1. Зайди на https://netlify.com
2. Нажми **"Add new site"** → **"Import an existing project"**
3. Выбери **GitHub** и найди свой репозиторий `criticism-app`
4. Настройки деплоя:
   - **Build command**: оставь пустым
   - **Publish directory**: оставь пустым или укажи `.`
5. Нажми **"Deploy site"**

### Шаг 3: Добавь API ключ

1. В Netlify перейди в **Site settings** → **Environment variables**
2. Нажми **"Add a variable"**
3. Добавь переменную:
   - **Key**: `ANTHROPIC_API_KEY`
   - **Value**: твой API ключ от Anthropic (sk-ant-api03-...)
4. Сохрани
5. Перейди в **Deploys** → **Trigger deploy** → **Clear cache and deploy site**

### Шаг 4: Настрой домен (опционально)

1. В Netlify перейди в **Domain settings**
2. Нажми **"Add custom domain"**
3. Введи `criticism-app.f5game.ru` (или любой поддомен)
4. Следуй инструкциям по настройке DNS

Приложение будет доступно по адресу:
- `https://ВАШ-САЙТ.netlify.app/`
- `https://criticism-app.f5game.ru/` (после настройки домена)

## 🔒 Безопасность

- ✅ API ключ хранится в переменных окружения Netlify (безопасно)
- ✅ Ключ не доступен в браузере
- ✅ Все запросы идут через serverless функцию

## 📝 Структура проекта

```
criticism-app-netlify/
├── index.html                    # Frontend приложения
├── netlify/
│   └── functions/
│       └── analyze.js            # Serverless функция для Claude API
├── netlify.toml                  # Конфигурация Netlify
├── package.json                  # Метаданные проекта
├── .env.example                  # Пример переменных окружения
├── .gitignore                    # Файлы для игнорирования Git
└── README.md                     # Эта инструкция
```

## 💰 Стоимость

- **Netlify**: Бесплатно (100 GB bandwidth, 125k requests/month)
- **Claude API**: ~$0.01-0.03 за один анализ ответа
- **Anthropic**: $5 бесплатно при регистрации

## 🛠 Локальная разработка (опционально)

Если хочешь тестировать локально:

```bash
npm install -g netlify-cli
netlify dev
```

Создай файл `.env` с твоим API ключом:
```
ANTHROPIC_API_KEY=your-key-here
```

Приложение откроется на `http://localhost:8888`

## 📞 Поддержка

При проблемах проверь:
1. API ключ правильно добавлен в Netlify
2. Функция `analyze.js` задеплоена
3. Консоль браузера (F12) на наличие ошибок
